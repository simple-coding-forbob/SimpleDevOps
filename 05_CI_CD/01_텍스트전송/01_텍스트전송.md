# 텍스트 전송: 깃허브 액션 -> AWS 컴퓨터

# Secrets 설정
Settings → Secrets and Variables → Actions에서 추가
EC2_HOST → EC2 퍼블릭 IP
EC2_USER → EC2 로그인 유저 (ubuntu)
EC2_SSH_KEY → PEM 파일 내용 (줄바꿈 포함)

# 프로젝트 구조
project
 ├─ hello.txt
 └─ .github
     └─ workflows
         └─ deploy.yml